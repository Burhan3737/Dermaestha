# 13 — Product Status Tracker

| Field            | Value                                                                                                                                                 |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| Document ID      | `13-PRODUCT_STATUS_TRACKER`                                                                                                                           |
| Status           | Canonical                                                                                                                                             |
| Version          | 1.28                                                                                                                                                  |
| Last updated     | 2026-07-02                                                                                                                                            |
| Sources absorbed | `server/src + client/src inspection; agentChangeLogs/2026-05-31-1700-m0-foundation-scaffold.md; ARCHITECTURE.md §5b; docs/specification/02; PRD §5.1` |
| Related docs     | 02, 03, 05                                                                                                                                            |

---

## Index

1. [Status legend & method](#1-status-legend--method)
2. [Milestone snapshot](#2-milestone-snapshot)
3. [Module-wise status](#3-module-wise-status)
4. [Feature-wise status](#4-feature-wise-status)
5. [Iteration roadmap](#5-iteration-roadmap)
6. [Remaining for v1](#6-remaining-for-v1)

---

## Purpose

This document reports the **actual build state** of the Dermestha codebase as of 2026-06-14 (post-Slice H · S7). Statuses are grounded in direct repo inspection (`server/src`, `client/src`, `prisma/`, `shared/`) cross-referenced against the M0 session log and the ARCHITECTURE §5b module inventory. It is the single source of truth for what has been built, what is stubbed, and what has not yet been started.

---

## 1. Status legend & method

| Status           | Meaning                                                                            |
| ---------------- | ---------------------------------------------------------------------------------- |
| Done             | File(s) confirmed in the repo with real implementation; not a stub or empty seam   |
| In progress      | Partial implementation or scaffolded seam exists; functional work remains          |
| Not started      | No corresponding file found in `server/src` or `client/src`                        |
| Deferred → v1.1  | Explicitly out of v1 scope per PRD §5.1; planned for the first post-launch release |
| Deferred → v1.2+ | Explicitly out of v1 and v1.1 scope per PRD §5.1                                   |
| Not verified     | Could not be confirmed or denied from repo inspection alone                        |

**Last verified:** 2026-06-14 (post-Slice H · S7). Statuses are grounded in repo inspection; anything not verifiable in code is marked `Not verified`. Stub files that only throw `AppError('NOT_IMPLEMENTED', ..., 501)` are counted as **In progress** (seam exists, real logic absent).

---

## 2. Milestone snapshot

| Milestone                      | Target        | Status      | % complete (estimate) | Notes                                                                                                                                                                          |
| ------------------------------ | ------------- | ----------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **M0 — Foundation / scaffold** | Pre-dev       | Done        | 100%                  | All 14 tasks complete per `agentChangeLogs/2026-05-31-1700-m0-foundation-scaffold.md`; server suite 20/20, client suite 2/2                                                    |
| **M1 — Booking flow**          | End of week 2 | In progress | ~90%                    | Slice A (auth) + B (discovery & availability) + C (booking + payment) merged; Slice E added the F07 notification outbox so confirmation + reminder emails enqueue in the booking `$transaction` and a real Resend adapter (key-based console fallback) replaces the stub. auth + doctor + availability + booking + manual-payment + cancellation services & routes, 30-min slot generation, `BLOCK_HAS_BOOKINGS` guard, client P-02/P-03/D-03/P-04/P-05/D-01/P-06/P-07/P-08/P-10 + 3 nav layouts. Remaining M1-ish polish (P-13/profile/landing) is M3/M4 |
| **M2 — Video + Payments**      | End of week 4 | In progress | ~100%                    | Manual-payment pivot (ADR-43): Payments are manual offline bank transfer with admin accept/reject; the PayFast gateway, signed webhook, Refund subsystem, reconciliation worker, refund-retry worker, and appointment-evaluation worker are all removed. Cancellation is built. The only cron is the `notification-dispatch` worker (F07.03, invalidation re-check + retry; in-process `node-cron`, ADR-27). Daily on the free tier is room + token only — no participant webhook. Slice H · S3 built the video consultation UI — lazy-loaded brand-themed Daily Prebuilt iframe + app chrome on P-11/P-12/D-04, plus the fire-and-forget client KPI emit seam `lib/analytics/track.js` (ADR-34) |
| **M3 — Prescriptions**         | End of week 6 | Done        | 100%                  | Slice F built the F08 prescription backend (immutable submit + snapshots; issued while `confirmed`, no state change — ADR-43), the F11 medicine backend (search + admin create/update), the `prescription_ready` outbox trigger, and the patient/doctor UI (P-09/P-13/D-05 + D-02 additions) incl. the client PDF renderer. Slice G's A-02 medicine UI closed M3's last open item; email template copy shipped in Slice H · S5 |
| **M4 — Launch-ready**          | End of week 8 | In progress | ~95%                  | Slice G built F10–F14 backends + A-01..A-05 views + DA5 reset; Slice H · S1 wired the PayFast Pakistan adapter, S2 the Daily.co adapter (ADR-33), S3 the video consultation UI (P-11/P-12/D-04 + the `track.js` KPI seam, ADR-34), S4 the public surface — P-01 landing (`/`) + F16 legal pages as a banner-marked structured DRAFT + the KPI #1 `landing_view`/`booking_started` emits (ADR-35), S5 the final plain-text email copy for the 7 templates (`email/templates.js`; refund/apology removed — ADR-43), and S6 the launch foundation — `POST /api/analytics/events` + writer + server-side `booking_confirmed`, DSN-gated Sentry with PII scrubbing (ADR-36), the AuditLog/Appointment indexes, automatic `Settings(id=1)` bootstrap, and the single-zod@3 dedupe (ADR-37). **S7 executed the E2E QA launch gate** — a Playwright harness (`e2e/`, `npm run test:e2e`; 6 Critical journeys J1–J6 vs mock adapters; ADR-38) + 3 new real-Postgres money-path integration tests; the gate surfaced and fixed an FK-`RESTRICT` crash class (4 defects, all Closed; ADR-39). Suite green: **320 server / 123 client / 11 e2e**. Verdict **Conditional-Go** (report: `docs/superpowers/reports/2026-06-14-v1-release-recommendation.md`). Remaining gates are human/vendor, not engineering: F16 final lawyer-reviewed legal copy, the vendor live-delivery gates (doc 07 §3/§10), and UAT sign-off (doc 09 §7). **Post-S7 (2026-06-15):** the three-role flow-audit fix cycle resolved 13 navigable-flow issues test-first (booking day-picker on P-03, P-07 payment-fail terminal state, doctor/admin + patient-desktop logout chrome, `/doctor/history` resolution, `/profile` account view, add-doctor photo-required, A-02 medicine edit, 404 page, cross-tenant Rx message, friendly doctor-history labels, anon `/auth/me`→200); suite now **322 server / 135 client / 17 e2e** green (+j7 future-day, +j8 logout-reachability, +j9 UI-states) |

---

## 3. Module-wise status

### Backend domain modules (ARCH §5b)

| #   | Module                      | Milestone | Status      | Evidence (actual file/path or "none found")                                                                                                    | Notes                                                                                                                                                                                                            |
| --- | --------------------------- | --------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Auth & session              | M1        | Built       | `server/src/middleware/session/session.js`, `server/src/middleware/requireRole/requireRole.js`, `server/src/middleware/rateLimit/rateLimit.js`, `server/src/lib/password/password.js` | Session/role middleware, rate-limiter, password hash/verify built (M0). Slice A added auth service + routes (signup/login/logout/me/forgot/reset/change), Zod validate, DA3 `mustChangePassword` gate, and per-account lockout — all tested. Slice G built the DA5 admin-mediated reset route (with doctor management). |
| 2   | User                        | M1        | Not started | none found                                                                                                                                     | `User` model exists in `prisma/schema.prisma`; no `user.service.js` or user routes found.                                                                                                                        |
| 3   | Doctor                      | M1/M4     | Built (M1)  | `server/src/modules/doctor/service.js`, `server/src/modules/doctor/controller.js`, `server/src/modules/doctor/index.js`                          | Slice B: public listing (active-only, paginated, no-leak card) + public profile (404-no-leak) + `getDoctorByUserId` for availability ownership. Slice G built admin create/edit/de-reactivate + DA5 reset + photo upload + the `PUT /api/doctors/:id/availability` route. |
| 4   | Availability                | M1        | Built       | `server/src/modules/doctor/service.js`, `server/src/modules/doctor/index.js`                                                             | Slice B: weekly recurring blocks (read/replace) + 30-min slot generation (date-fns-tz Karachi↔UTC, ADR-21), lead-time filter, active-appointment exclusion, `BLOCK_HAS_BOOKINGS` orphan guard. Tested (9 unit + integration). |
| 5   | Slot & booking              | M1→M2     | Built       | `server/src/modules/appointment/service.js`, `server/src/modules/appointment/controller.js`, `server/src/modules/appointment/index.js`              | Slice C: `lockSlot` creates `slot_locked` (10-min hold) with Single-Lock + No-Overlap + reclaim-on-conflict; lazy expiry (ADR-23). `POST /api/appointments/lock`. Pending-hold recovery (34f978d): a live hold now surfaces as a "Payment pending" card with "Complete payment" in the patient appointments list + a booking-page "Go to your pending booking" recovery link. Tested (5 unit + integration).                 |
| 6   | Payment                     | M2        | Built       | `server/src/modules/appointment/` (lock + `/pay`), `server/src/modules/admin/` (accept/reject) | Manual offline bank-transfer model (ADR-43, supersedes the PayFast gateway): `POST /api/appointments/lock` creates a `pending` appointment + `feeAtBooking` snapshot (#6); `GET /api/appointments/:id` returns `paymentInstructions` (bank details from Settings); `POST /api/appointments/:id/pay {reference}` records `paymentReference`/`paymentSubmittedAt` + enqueues `payment_submitted_admin`; admin `POST /api/admin/appointments/:id/accept` (→`confirmed`, `booking_confirmation`, `booking_confirmed` analytics) / `/reject` (→`cancelled`, `payment_not_received`). The PayFast adapter, signed webhook, payment-intent, `Payment` table, and reconciliation worker are removed. Tested. |
| 7   | Appointment + state machine | M2        | Built       | `server/src/modules/appointment/service.js`                                                                                              | Single transition writer (legal-pair validation + audit). 3-state model (ADR-43): `pending→confirmed` (admin accept), `pending`/`confirmed`→`cancelled` (admin reject, patient/doctor cancel). No `in_progress`/`completed`/no-show transitions and no appointment-evaluation worker. Tested. |
| 8   | Refund                      | M2        | Removed (ADR-43) | `server/src/modules/appointment/service.js`                                                                                                        | REMOVED (ADR-43): the refund subsystem is removed with the payment gateway — no in-app refunds, no net-of-fee quote, no `Payment.refundAttempts`/`retryDueRefunds` worker. Cancellation simply frees the slot; offline payment is settled out-of-band. |
| 9   | Video                       | M2        | Built       | `server/src/integrations/video/`, `server/src/modules/video/service.js`, `server/src/modules/appointment/index.js` (`GET /:id/video-token`), `client/src/modules/video/` | Video-token issuance with a slot-window guard (`VIDEO_WINDOW_CLOSED`), confirmed-only. Daily is used on the **free tier** as room + token only — NO participant webhook, no join recording, and no no-show / appointment-evaluation worker (ADR-43). Slice H · S3: video consultation UI built (P-11/P-12/D-04) — client `useDailyCall` lazy-loads `@daily-co/daily-js` and mounts a brand-themed Daily Prebuilt iframe; the dev `joinSimUrl` mock path is retained for offline/CI (ADR-34).                                                                                   |
| 10  | Prescription                | M3        | Built       | `server/src/modules/prescription/service.js`, `server/src/modules/prescription/controller.js`, `server/src/modules/prescription/index.js`; `client/src/modules/prescription/` (`views/PrescriptionBuilder`, `views/PrescriptionView`, `usePrescription.js`) | Slice F: immutable submit in one `$transaction` (doctor + item name/price snapshots #3/#5, free-text price `null`) → issued while the appointment is `confirmed`, leaving its state unchanged (ADR-43; formerly `completed→prescription_issued`) → `prescription_ready` enqueue (`dedupeKey`=rx id); chronological read; 404-no-leak owner gates. Tested (incl. row-lock race + correction integration). |
| 11  | Medicine catalogue          | M3        | Built       | `server/src/modules/medicine/service.js`, `server/src/modules/medicine/controller.js`, `server/src/modules/medicine/index.js`                  | Slice F: `GET /api/medicines?search=` (active-only, name-sorted, search on name+genericName) + admin `POST`/`PATCH /api/admin/medicines` (deactivate-only via `isActive`; audit `medicine.created`/`medicine.updated`). Slice G built the A-02 admin catalogue UI (`includeInactive=true` is admin-only). Tested. |
| 12  | Cancellation                | M2        | Built       | `server/src/modules/appointment/service.js`, `server/src/modules/appointment/index.js` (`POST /:id/cancel`)                                         | Patient or doctor cancel → `cancelled` from `pending`/`confirmed` (ADR-43); slot released; `cancellation` email. No window, no refund, no `cancelled_refunded`/`cancelled_no_refund`/`doctor_cancelled` states. Both roles tested; doctor-side UI (D-06) built. |
| 13  | Notification/email          | M1→M4     | Built (v1 transport) | `server/src/modules/notification/service.js`, `server/src/integrations/email/resend.js`, `server/src/integrations/email/console.dev.js`, `server/src/integrations/email/index.js`             | Slice E: `notification_jobs` outbox + `enqueue`/`enqueueBookingEmails` (cadence + short-lead skip, F07.02), `dispatchDueNotifications` worker (invalidation re-check + backoff + exhaustion alert, F07.03); real Resend HTTP adapter with `RESEND_API_KEY`-based console fallback. Slice H · S5: final plain-text copy shipped via the shared `email/templates.js` `render()` for the 7 templates (`payment_submitted_admin`, `booking_confirmation`, `payment_not_received`, `cancellation`, `prescription_ready`, `reminder_24h`, `reminder_1h` — refund/apology emails removed per ADR-43). |
| 14  | Audit log                   | M2→M4     | Built       | `server/src/services/audit/audit.service.js`, `server/test/unit/services/audit/audit.service.test.js`                                                            | Append-only `record()` writer built and tested (2/2 tests green). Slice G built the admin query API + 11 new Slice G event types.                                                                                 |
| 15  | Admin alerts / health       | M4        | Built       | `server/src/modules/admin/`                                                                                                                    | Slice G built the F12 alert feed (4 audit-sourced kinds + `awaiting_prescription` derived, both capped 100), the email-resend remediation, and the A-03 view.                                                     |
| 16  | Records & audit search      | M4        | Built (intentional UI gaps) | `server/src/modules/admin/`                                                                                                    | Slice G built records + audit query + the detail view + dispute set/clear; the audit-tab filter controls and the records `state` filter UI are intentionally deferred.                                            |
| 17  | Settings                    | M4        | Built       | `server/src/modules/admin/`                                                                                                                    | Slice G built the settings service + `GET`/`PUT` singleton routes (id=1, bounded full-replace, `before`→`after` `settings.updated` audit, live read) + the A-05 view.                                             |
| 18  | Analytics/telemetry         | M2→M4     | Built       | `server/src/modules/analytics/`, `client/src/lib/analytics/track.js`                                                                          | `AnalyticsEvent` model in schema. Slice H · S6 shipped the server side: `POST /api/analytics/events` (public, rate-limited 60/min/IP, body validated against the closed doc 14 §6 catalog — unknown `type` → `400 VALIDATION_FAILED`, success `202 { ok: true }`), a best-effort `analytics.record` writer that never throws into the request path, and the server-side terminal `booking_confirmed` emit in `payment/service.js` `confirmPaidAppointment` (both webhook + reconciliation confirm paths). Client emit seam `track.js` (Slice H · S3/S4, ADR-34) fires `video_join_attempt`/`video_join_success` (KPI #3) + `landing_view`/`booking_started` (KPI #1), fire-and-forget. The full KPI funnel now lands. |
| 19  | Legal content               | M4        | Built (DRAFT) | `client/src/modules/legal/` (`legal.routes.jsx`, `components/LegalPage/`, `views/Terms`, `views/Privacy`)                                       | Slice H · S4: public/unauthenticated `/legal/terms` + `/legal/privacy` rendered through one reusable `LegalPage` template (brand topnav + persistent DRAFT banner + structured `{ heading, body }` sections; ADR-35). Placeholder copy only — **final lawyer-reviewed copy is a pre-launch gate**. Privacy page cross-references the doc 08 §2 data-handling policy. |

### Background workers (ARCH §5b)

| Worker                                     | Milestone | Status      | Evidence   | Notes                                            |
| ------------------------------------------ | --------- | ----------- | ---------- | ------------------------------------------------ |
| Reconciliation (hourly PayFast query)      | M2        | Removed (ADR-43) | none (removed) | REMOVED (ADR-43): no payment gateway or webhook in the manual model, so no reconciliation worker. |
| Notification (email dispatch + retry)      | M2        | Built       | `server/src/workers/index.js`, `server/src/modules/notification/service.js` | Slice E: `dispatchDueNotifications(now)` (`* * * * *`) — due outbox rows, invalidation re-check, exponential backoff, exhaustion alert; `/dev/worker/notifications` trigger (ADR-27). This is the single remaining cron job (`notification-dispatch`); reconciliation, refund-retry, and appointment-evaluation workers are removed (ADR-43). |
| Refund-retry (backoff + exhaustion)        | M2        | Removed (ADR-43) | none (removed) | REMOVED (ADR-43): no refunds in the manual model, so no refund-retry worker. |
| Appointment-evaluation (state advancement) | M2        | Removed (ADR-43) | none (removed) | REMOVED (ADR-43): the 3-state model has no `in_progress`/`completed`/no-show transitions, so there is no appointment-evaluation worker. |

### Cross-cutting / infra

| Item                            | Status      | Evidence                                                                                                                                |
| ------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| Role middleware (DA6)           | Done        | `server/src/middleware/requireRole/requireRole.js` — tested (3/3)                                                                                   |
| Rate-limiter factory            | Done        | `server/src/middleware/rateLimit/rateLimit.js`                                                                                                    |
| Error envelope middleware       | Done        | `server/src/http/errorHandler/errorHandler.js`, `server/src/http/AppError.js` — tested (3/3)                                                         |
| Zod env validation              | Done        | `server/src/config/env/env.js` — tested (3/3)                                                                                               |
| Prisma singleton                | Done        | `server/src/lib/prisma/prisma.js`                                                                                                              |
| Structured logger               | Done        | `server/src/lib/logger/logger.js`                                                                                                              |
| Error-tracking seam             | Built       | `server/src/lib/errorTracking/errorTracking.js` — Slice H · S6 shipped **Sentry** (`@sentry/node`), DSN-gated on `SENTRY_DSN` (logging no-op without a DSN), `sendDefaultPii:false` + `beforeSend` PII scrub (request body/cookies/auth headers/user identity removed), `initErrorTracking()` at boot; `captureException` runs alongside the Slice G `errorHandler`→audit bridge (`system.unhandled_exception`), not feeding A3 (ADR-36) |
| Password hash/verify (argon2id) | Done        | `server/src/lib/password/password.js` — tested (2/2)                                                                                             |
| Prisma schema + migrations      | Done        | `prisma/schema.prisma`, `prisma/migrations/20260531163617_init/migration.sql` (includes hand-appended `uniq_active_slot` partial index). Slice H · S6 added `20260613213051_slice_h_s6_indexes` — `appointments.slot_start` + `audit_log.target_ref` query indexes (doc 04 §4c/§4d) |
| 12-factor config loader         | Done        | `server/src/config/env/env.js`, `server/src/config/constants.js`                                                                            |
| Dockerfile + docker-compose     | Done        | `Dockerfile`, `docker-compose.yml`                                                                                                      |
| Payment adapter interface       | Removed (ADR-43) | REMOVED (ADR-43): no payment gateway or adapter in the manual offline-payment model. Payment is offline bank transfer with admin accept/reject (module 6); the PayFast adapter, the `verify-return` / `record-refund` routes, and the `payment.manual_review_required` / `payment.refund_manual_required` alerts are deleted. |
| Video adapter interface         | Built (Daily.co, room+token only) | `server/src/integrations/video/index.js` + `daily.js` (real Daily.co adapter — room + token only) + `daily.mock.js` (dev, ADR-24) + `daily.stub.js`. Free-tier usage: slot-bounded rooms via `createRoom({ notAfterIso })` + meeting tokens; the `POST /api/webhooks/daily` participant webhook and its HMAC verification are removed (ADR-43). Opt-in via `VIDEO_PROVIDER=daily`; live-delivery gated by doc 07 §10. Client video UI (P-11/P-12/D-04) built in Slice H · S3 (ADR-34). |
| Email adapter interface         | Built       | `server/src/integrations/email/index.js` (key-based `pickProvider`) + `resend.js` (real HTTP adapter) + `console.dev.js` (dev fallback). Slice E: `RESEND_API_KEY` selects the real adapter, else console + loud warning; `resend.stub.js` deleted. Verified-domain + `RESEND_FROM` needed for patient inboxes (doc 14). |
| Shared Zod DTO seam             | Done        | `shared/schemas/auth` + `doctor` + `appointment` + `medicine` + `prescription` + `index.js` — auth (Slice A) + availability/doctor-list/slots (Slice B) + lock/cancel (Slice C) + medicine search/create/update + prescription submit DTOs (Slice F); all v1 admin DTOs now complete (Slice G)                                               |
| `/api/health` route             | Done        | `server/src/health/index.js` — Prisma `SELECT 1` liveness check                                                                        |
| Static SPA serving + catch-all  | Done        | `server/src/index.js`                                                                                                                   |
| Admin bootstrap script          | Done        | `prisma/scripts/bootstrap-admin.js`                                                                                                     |
| DB seed                         | Done        | `prisma/seed.js`                                                                                                                        |
| E2E test harness                | Built       | `playwright.config.js`, `e2e/` (`tests/`, `support/`, `global-setup.js`); root `npm run test:e2e`; `@playwright/test` root devDependency. Slice H · S7: Playwright launch gate — 6 Critical journeys J1–J6 (**11/11**) driving a real browser against the app booted with the mock adapters + a seeded DB (ADR-38). The gate surfaced an FK-`RESTRICT` money-path crash class, fixed under ADR-39 and re-verified by 3 new real-Postgres integration tests (`server/test/integration/`). Suite: 320 server / 123 client / 11 e2e |

### Frontend modules

| Group                                                         | Status      | Evidence                                                                                                                     | Notes                                                |
| ------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| Design tokens (`tokens.css`)                                  | Done        | `client/src/styles/tokens.css` — verbatim port from mockups                                                                  |
| Component CSS (`components.css`)                              | Done        | `client/src/styles/components.css` — verbatim port from mockups                                                              |
| App entry (`main.jsx`, `App.jsx`)                             | Done        | `client/src/main.jsx`, `client/src/App.jsx` — providers composed in `client/src/context/AppProviders.jsx` (QueryClient + BrowserRouter + Session); `AppRoutes` with role placeholders |
| Route config seam (`routes.jsx`)                              | In progress | `client/src/routes.jsx` (`buildRoutes(session)` aggregating each module's `*.routes.jsx`) + `App.jsx` — marketing + legal + auth + discovery + booking routes wired (`/` public landing with logged-in-patient→`/browse` redirect, `/browse` doctor listing [relocated from `/`], `/legal/terms`, `/legal/privacy`, `/doctors/:id`, `/doctor/availability`; patient-gated `/book/:id`, `/pay/return`, `/appointments`); A-01..A-05 wired behind `RoleRoute(admin)`, with `/admin` → `/admin/doctors` as the default | 
| Client-side role guard (`RoleRoute.jsx`)                      | Done        | `client/src/lib/RoleRoute/RoleRoute.jsx` — tested (2/2)                                                                                |
| 24 views (P-01…P-13, D-01…D-06, A-01…A-05)                    | Done | P-04, P-05 (+ forgot/reset), D-01 (Slice A); P-02, P-03, D-03 (Slice B); P-06, P-07, P-08, P-10 (Slice C); P-09 past appointments, P-13 prescription view, D-05 prescription builder + D-02 write-prescription action/awaiting badge (Slice F); A-01 doctors, A-02 medicines, A-03 system health, A-04 records & audit, A-05 settings (Slice G); P-11 pre-call get-ready room, P-12 patient video room, D-04 doctor video room + the P-08/D-02 Join→`/ready` redirect & `video_join_attempt` emit (Slice H · S3); P-01 landing (Slice H · S4) | 24 of 24 built; P-01 landing was the last, built in Slice H · S4 (`client/src/modules/marketing/`). The D-02 today's-appointments base view (Today/History tabs, today's list, 10-min Join gate, write-Rx action, awaiting badge) and the D-06 doctor cancel modal (`DoctorCancelModal`, wired in `DoctorToday`) were already built and are confirmed; S3 added only D-02's `/ready` Join redirect + `video_join_attempt` emit |
| Layouts (`TopNavLayout`, `BottomTabsLayout`, `SidebarLayout`) | Done        | `AuthSplitLayout` (Slice A); `PatientLayout` (topnav + mobile tabbar) + `SidebarLayout` (Slice B)                            | All 3 chrome layouts built; AuthSplit for auth pages |
| Shared design components (~16)                                | In progress | `Button, Field, Card, Alert, Checkbox` (Slice A); `DoctorCard, SlotButton` (Slice B); `CancelModal` (Slice C)                | 8 of ~16 built; rest as views need them              |
| Typed API client                                              | Done        | `client/src/lib/apiClient/apiClient.js` — tested (3/3)                                                                                 | error-envelope wrapper; single network seam          |
| Auth/session context                                          | Done        | `client/src/context/session/session.jsx` — tested (2/2)                                                                                  | React Context, **state only** (session/loading/refresh/setSession); hydrates via `/auth/me`. One-shot auth actions in `modules/auth/useAuth.js` |
| Video chrome (Daily SDK wrapper)                              | Built (real Daily SDK in S3) | `client/src/modules/video/{useDailyCall.js, views/VideoRoom/, views/WaitingRoom/}`                                          | Slice D shipped only **placeholder** app-rendered chrome with **NO** video SDK. Slice H · S3 landed the real integration: `useDailyCall` lazy-imports `@daily-co/daily-js`, mounts a brand-themed Daily **Prebuilt iframe** into one shared role-aware `VideoRoom` (P-12/D-04), and `WaitingRoom` (P-11) is the get-ready route. Daily owns the in-call tiles/controls/device pickers/reconnection; the app owns the surrounding chrome (countdown, doctor 5-min warning, slot-end+5m cutoff, leave) (ADR-34). The Slice D mock `joinSimUrl` path is retained for offline/CI |
| Client-side PDF renderer                                      | Built       | `client/src/lib/pdf/renderPrescriptionPdf.js`                                                                                | Slice F: client-only boundary; `pdf-lib` lazily imported into a separate build chunk (main bundle unchanged) |
| Legal page templates                                          | Built (DRAFT) | `client/src/modules/legal/components/LegalPage/LegalPage.jsx`                                                                | Slice H · S4: reusable F16 `LegalPage` (brand topnav + persistent DRAFT banner + structured `{ heading, body }` sections) backing `/legal/terms` + `/legal/privacy`; placeholder copy, final lawyer copy is a pre-launch gate (ADR-35) |

---

## 4. Feature-wise status

Feature IDs are those defined in `docs/specification/02-SCOPE_FEATURE_DOCUMENT.md`.

| Feature                                              | Owning module(s)                            | Milestone | Status      | Notes                                                                                                        |
| ---------------------------------------------------- | ------------------------------------------- | --------- | ----------- | ------------------------------------------------------------------------------------------------------------ |
| F01 — Patient authentication & account               | Auth & session (1), User (2)                | M1        | In progress | Auth routes + service (signup/login/logout/me/forgot/reset/change) built & tested (Slice A); reset email delivery live via the Slice E Resend adapter; no email verification in v1 by design; patient profile/account view later |
| F02 — Doctor discovery (public listing & profile)    | Doctor (3)                                  | M1        | Built       | Slice B: `GET /api/doctors` (paginated, active-only, no-leak card + next-slot hint), `GET /api/doctors/:id` (404-no-leak), P-02/P-03 views; tested |
| F03 — Slot booking & slot-lock                       | Slot & booking (5), Availability (4)        | M1→M2     | Built       | Slice C: `POST /api/appointments/lock` → `pending` (10-min hold) + who-for (P8); Single-Lock/No-Overlap; lazy expiry + reclaim-on-conflict over `uniq_active_slot` (ADR-23); P-06 booking view. Pending-hold recovery (34f978d): an abandoned hold now surfaces as a "Payment pending" card with "Complete payment" in the patient appointments list + a booking-page "Go to your pending booking" recovery link. Tested |
| F04 — Payment                                        | Payment (6), Appointment state machine (7)  | M2        | Built       | Manual offline bank transfer (ADR-43): `lock`→`pending` + `feeAtBooking` (#6); `GET /:id` returns `paymentInstructions` (bank details from Settings); `POST /:id/pay {reference}` records `paymentReference`/`paymentSubmittedAt` + enqueues `payment_submitted_admin`; admin `accept`→`confirmed` (`booking_confirmation` + `booking_confirmed` analytics) / `reject`→`cancelled` (`payment_not_received`). PayFast adapter, signed webhook, payment intent, `Payment` table, and reconciliation worker removed. P-07 payment-instructions view. Tested |
| F05 — Appointment lifecycle & video consultation     | Appointment + state machine (7), Video (9)  | M2        | Built       | 3-state model (ADR-43): no `in_progress`/`completed`/no-show states, no appointment-evaluation worker. Video-token issuance + slot-window guard, confirmed-only; Daily on the free tier is room + token only — NO participant webhook and no join recording. Joining the room does not change appointment state. Tested |
| F06 — Cancellation                                   | Cancellation (12)                           | M2        | Built       | `POST /api/appointments/:id/cancel` — patient/doctor cancel → `cancelled` from `pending`/`confirmed` (ADR-43); slot released; `cancellation` email. No window and no refund (refund subsystem removed). P-08 dashboard + P-10 modal; doctor UI (D-06) built. Tested |
| F07 — Reminders & notifications                      | Notification/email (13)                     | M1→M4     | Built (v1 transport) | Slice E: `notification_jobs` transactional outbox; event emails enqueue in the caller's `$transaction`; confirmation + slot−24h/slot−1h reminders (short-lead skip, Karachi-correct); `dispatchDueNotifications` worker re-checks state (suppress invalidated reminders), retries with backoff, alerts on exhaustion; payment-submitted / booking-confirmation / payment-not-received / cancellation triggers wired (refund + apology emails removed — ADR-43). Real Resend adapter + console fallback. Final template copy is M4 |
| F08 — Prescription                                   | Prescription (10), Medicine (11)            | M3        | Built       | Slice F: immutable submit + doctor/item snapshots (#3/#5), issued while `confirmed` leaving state unchanged (ADR-43; formerly `completed→prescription_issued`), `prescription_ready` enqueue (dedupeKey=rx id), chronological read; corrections = new linked row (state unchanged); client-render PDF (P-13) re-downloadable. P-09/P-13/D-05 + D-02 action/badge. Tested |
| F09 — Doctor weekly availability                     | Availability (4)                            | M1        | Built       | Slice B: `GET /api/doctors/:id/availability` (doctor-own/admin), `PUT /api/availability` (replace, `BLOCK_HAS_BOOKINGS` guard), `GET /api/doctors/:id/slots` (30-min gen), D-03 grid; tested |
| F10 — Admin: doctor onboarding, edit, (de)activation | Doctor (3)                                  | M1/M4     | Built       | Slice G: admin create (status/isActive split + `mustChangePassword`), edit (`409 IMMUTABLE_FIELD` on pmc/email), de-/reactivate, DA5 reset, photo upload, `PUT /api/doctors/:id/availability`, A-01 view. Tested |
| F11 — Admin: medicine catalogue                      | Medicine (11)                               | M3        | Built       | Slice F: `GET /api/medicines?search=` (active-only, name+genericName) + admin `POST`/`PATCH /api/admin/medicines` (deactivate-only via `isActive`; audit rows); never alters existing prescription snapshots (#5). Slice G built the A-02 admin catalogue UI (admin-only `includeInactive`). Tested |
| F12 — Admin: system-health alerts                    | Admin alerts / health (15)                  | M4        | Built       | Slice G: alert feed (4 audit-sourced kinds + `awaiting_prescription` derived, both capped 100), email-only resend, A-03 view, and the `errorHandler`→audit `system.unhandled_exception` bridge. Tested |
| F13 — Admin: records & audit log (unified)           | Records & audit search (16), Audit log (14) | M4        | Built (intentional UI gaps) | Slice G: records + audit query + detail view + dispute set/clear + email resend (A-04); audit-tab filter + records `state` filter UI deferred. Tested |
| F14 — Admin: platform settings                       | Settings (17)                               | M4        | Built       | Slice G: settings service + `GET`/`PUT` singleton (id=1, bounded full-replace, `before`→`after` `settings.updated` audit) + A-05 view. Tested |
| F15 — Doctor & admin authentication & roles          | Auth & session (1)                          | M1        | Built       | Shared login + role routing, DA3 forced-change gate, `requireRole` all built & tested (Slice A); Slice G built DA5 admin-mediated reset + admin doctor-create; admin bootstrap script present (DA4)           |
| F16 — Legal content (ToS / Privacy)                  | Legal content (19)                          | M4        | Built (DRAFT) | Slice H · S4: public `/legal/terms` + `/legal/privacy` via the reusable `LegalPage` template (banner-marked structured DRAFT; ADR-35), linked from the sign-up consent checkbox + landing footer; Privacy cross-refs the doc 08 §2 data-handling policy. Final lawyer-reviewed copy is a pre-launch gate. F16.02 consent capture (`tos_accepted_at`) was built in Slice A |

---

## 5. Iteration roadmap

### v1 (M1–M4, 8-week scope)

| Milestone                      | Deliverable                                                                                                                 | Status      |
| ------------------------------ | --------------------------------------------------------------------------------------------------------------------------- | ----------- |
| M1 — Booking flow (Week 2)     | Patient sign-up + ToS consent, login (shared), doctor listing, slot booking (no payment), confirmation email                | In progress |
| M2 — Video + Payments (Week 4) | Full video consultation (mobile-tested on 3G); manual offline bank-transfer payment + admin accept/reject (ADR-43)            | In progress |
| M3 — Prescriptions (Week 6)    | Doctor prescription builder + patient-ID header; medicine catalogue prices; patient itemised PDF download                   | Done        |
| M4 — Launch-ready (Week 8)     | Admin panel (doctor onboarding, medicine catalogue, alert feed); landing page; email automation; legal content; E2E QA **executed** (Slice H · S7 launch gate, Conditional-Go) | In progress |

### Technical follow-ups (pre/at-launch — post-Slice G)

Engineering hygiene surfaced during the Slice G build + review; these are not product features. Full detail lives in the cross-referenced docs; this table is the actionable tracker.

| Item                                                                                                                                                      | Target                     | Detail                       | Status |
| --------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ---------------------------- | ------ |
| Add `AuditLog @@index([targetRef])` + `Appointment @@index([slotStart])` (migration) — admin audit / records / alert queries currently full-scan          | Before traffic grows       | doc 04 §4d; doc 07 §2.3       | **Resolved** (Slice H · S6) — migration `20260613213051_slice_h_s6_indexes` |
| `Settings(id=1)` singleton bootstrap on first prod deploy — no automated migrate/seed step exists; GET returns `null` and PUT throws without the row       | Before / at first prod deploy | doc 10 §3; doc 07 open-q 7 | **Resolved** (Slice H · S6) — `ensureSettings()` idempotent upsert at boot (`server/src/index.js`) |
| Align on a single Zod version (root `zod@4` vs server `zod@3`; `errorHandler` duck-types `ZodError` as a workaround)                                        | Before next milestone      | doc 15 §7; doc 07 §2.3        | **Resolved** (Slice H · S6) — single `zod@3` via `shared` workspace + root `overrides.zod ^3.23.0` (transitive zod@4 collapsed); duck-typing removed (ADR-37) |
| Money-path FK-delete crash class — deleting a `slot_locked` appointment a `Payment` FK-references (`ON DELETE RESTRICT`) raised `P2003` on the `payment.failed`, reconcile-failed, lazy-reclaim, and edge-#6a refund paths | At launch gate (S7)        | doc 11 ADR-39; doc 04 §2f/§2n | **Resolved** (Slice H · S7) — no-cascade release policy (ADR-39): force-expire the lock instead of deleting; reclaim only `failed`/absent blockers; proven by 3 new real-Postgres integration tests. Code search confirms no remaining instance |

### v1.1 (2–4 weeks post-launch)

| Item                                                | Status          |
| --------------------------------------------------- | --------------- |
| SMS / WhatsApp notifications                        | Deferred → v1.1 |
| Live queue / spot booking (removes lead-time floor) | Deferred → v1.1 |
| Pre-consultation skin photo upload                  | Deferred → v1.1 |
| Patient account deletion / data-export flow         | Deferred → v1.1 |
| ToS / Privacy versioning + re-prompt on update      | Deferred → v1.1 |
| Doctor self-service password reset (email token)    | Deferred → v1.1 |

### v1.2+

| Item                                                    | Status           |
| ------------------------------------------------------- | ---------------- |
| Server-side PDF generation (email-attached, signed)     | Deferred → v1.2+ |
| Dermestha wallet (instant refunds)                      | Deferred → v1.2+ |
| Family profiles / sub-accounts                          | Deferred → v1.2+ |
| Secondary bank gateway (UBL/HBL)                        | Deferred → v1.2+ |
| Medicine Ordering Module (in-app order + home delivery) | Deferred → v1.2+ |
| Urdu language support                                   | Deferred → v1.2+ |
| Native iOS / Android apps                               | Deferred → v1.2+ |

### Medicine Ordering Module (separate scope — §6)

Listed in PRD §6 as a **separately scoped and costed module**, explicitly NOT part of the v1 8-week build. v1 ships only the prescription-side prerequisites (admin medicine prices, computed prescription total, itemised self-pay PDF). The module is **Deferred → v1.2+** and requires its own planning, costing, and timeline discussion before scheduling.

---

## 6. Remaining for v1

This section is the v1 build backlog (baseline 2026-06-01), reconciled as each slice ships: `[x]` built, `[~]` partial, `[ ]` remaining. Unchecked items are the true outstanding work for the v1 8-week scope.

### M1 — Booking flow

- [x] Auth service: sign-up (name, email, phone, password, ToS consent capture to `tos_accepted_at`) — Slice A
- [x] Auth service: login (shared `/login`; role-based routing on success to patient/doctor/admin dashboard) — Slice A
- [x] Auth service: forgot-password (enumeration-safe; 1-hour token; Resend integration) — Slice A (real Resend delivery Slice E)
- [x] Auth routes: `POST /api/auth/signup`, `POST /api/auth/login`, `POST /api/auth/logout`, `POST /api/auth/forgot-password`, `POST /api/auth/reset-password` — Slice A
- [x] DA3 forced-first-login-change middleware gate (checks `must_change_password`) — Slice A
- [x] Doctor service: public listing (active-only, paginated) and public profile endpoint — Slice B
- [x] Doctor routes: `GET /api/doctors`, `GET /api/doctors/:id` — Slice B
- [x] Availability service: save/update weekly recurring blocks; generate 30-minute slots from blocks; lead-time filter; block-with-bookings guard — Slice B
- [x] Availability routes: `GET /api/doctors/:id/slots`, `GET /api/doctors/:id/availability`, `PUT /api/availability` (doctor-scoped) — Slice B (replace is `PUT /api/availability`, not the per-id form originally sketched)
- [x] Booking / slot-lock service: create `pending` appointment; 10-minute hold; single-lock + no-overlap; lazy expiry + reclaim-on-conflict (ADR-23) — Slice C
- [x] Booking routes: `POST /api/appointments/lock`, `POST /api/appointments/:id/pay` — Slice C (no `DELETE …/lock`; expiry is lazy, not an explicit release route)
- [x] Notification service: `notification_jobs` outbox + dispatch worker (cadence, short-lead skip, invalidation, backoff); payment-submitted/booking-confirmation/payment-not-received/cancellation triggers (refund/apology removed — ADR-43); real Resend adapter + console fallback — Slice E. Final template copy is M4
- [~] Shared Zod DTOs: sign-up, login (Slice A); lock + cancel (Slice C); doctor list/slots (Slice B)
- [~] Patient views (client): P-02 ✓, P-03 ✓, P-04 ✓, P-05 ✓ (Slice A/B); P-06 booking ✓, P-07 payment return ✓, P-08 upcoming ✓, P-10 cancel modal ✓ (Slice C)
- [x] Doctor views (client): D-03 weekly availability grid (Slice B); D-01 forced first-login password change (Slice A)
- [x] Layout chromes: `PatientLayout` (TopNav + BottomTabs), `SidebarLayout` (doctor/admin) — Slice B
- [~] Shared design components (~16 from `_component-reference.html`): `Button`, `Card`, `Input`, `Modal`, slot-grid, etc. — ~9/16 built (see §3 frontend; `Pagination` added Slice G); rest land as views need them
- [x] Typed API client (`client/src/lib/apiClient/apiClient.js`) — Slice A (`api.patch` + multipart `api.upload` added Slice G)
- [x] Auth/session context provider (React) — Slice A (`client/src/context/session/session.jsx`)
- [x] Route config: wire all M1 views with `RoleRoute` guards — Slice A/B/C (admin A-01..A-05 added Slice G)

### M2 — Video + Payments

- [x] Payment service (manual offline, ADR-43): `lock`→`pending` + `feeAtBooking` snapshot, `paymentInstructions` (bank details from Settings), `/pay {reference}` submission, admin accept/reject. No payment intent, signed webhook, `Payment` table, or reconciliation worker
- [x] Payment routes: `POST /api/appointments/:id/pay {reference}`, `GET /api/appointments/:id` (paymentInstructions), `POST /api/admin/appointments/:id/accept`|`/reject` — manual model (ADR-43); no `/webhooks/payfast` or `/reconcile`
- [~] Video service: room creation + time-bound token issuance + participant-event ingestion — Slice D (against the stub adapter; the concrete Daily.co network adapter is Slice H)
- [x] Video routes: `POST /api/appointments/:id/video/token` — Slice D
- [x] Appointment state-machine service: 3-state transition validation + audit-log writes; side-effect triggers (email) — `pending→confirmed`/`→cancelled` only (ADR-43; no refund trigger, no video-side transitions)
- [x] Appointment routes: `GET /api/appointments` (patient/doctor scoped), `GET /api/appointments/:id` — Slice C
- [ ] ~~Refund service~~ — REMOVED (ADR-43): no in-app refunds in the manual offline-payment model; the refund subsystem and refund-retry worker are deleted with the gateway
- [x] Cancellation service: patient and doctor cancel → `cancelled` (no window, no refund — ADR-43) — Slice C
- [x] Cancellation route: `POST /api/appointments/:id/cancel` (unified patient/doctor by session role) — Slice C
- [x] Workers directory (`server/src/workers/`): the single `notification-dispatch` worker (`* * * * *`) — Slice E. Reconciliation, refund-retry, and appointment-evaluation workers are removed (ADR-43). In-process `node-cron`; `/dev/worker/*` on-demand triggers
- [x] Analytics: server `POST /api/analytics/events` route (public, rate-limited 60/min/IP, closed-catalog validated) + `analytics.record` best-effort writer + server-side `booking_confirmed` emit — Slice H · S6; client KPI #3 seam (`track.js`, `video_join_attempt`/`video_join_success`) — Slice H · S3 (ADR-34); KPI #1 (`landing_view`/`booking_started`) — Slice H · S4. Full funnel now lands
- [x] Audit log query API (admin-only): `GET /api/admin/audit` — Slice G (with `GET /api/admin/records` + `/records/:id`)
- [x] Patient views: P-07 payment return ✓, P-08 upcoming (with "Join Call") ✓ (Slice C), P-09 past appointments ✓ (Slice F); P-11 pre-call get-ready room ✓, P-12 patient video room ✓ (Slice H · S3)
- [x] Doctor views: D-03 availability grid ✓ (Slice B); D-02 appointments view ✓ (time-based Upcoming/Past, 10-min Join gate, write-Rx, awaiting badge, cancel modal, inert pending rows; ADR-45); D-04 doctor video room ✓ (Slice H · S3)

### M3 — Prescriptions

- [x] Prescription service: immutable submit, items + price snapshot, patient-ID snapshot, chronological list, JSON read API — Slice F
- [x] Prescription routes: `POST /api/appointments/:id/prescriptions`, `GET /api/appointments/:id/prescriptions` — Slice F
- [x] Medicine service: admin create/update, unit price, deactivate (via `isActive` toggle; no DELETE) — Slice F (backend; A-02 admin UI built in Slice G)
- [x] Medicine routes: `GET /api/medicines`, `POST /api/admin/medicines`, `PATCH /api/admin/medicines/:id` — Slice F
- [x] Client-side PDF renderer (`renderPrescriptionPdf(json)` boundary in `client/src/lib/pdf/`) — Slice F
- [x] Prescription-ready email trigger (Resend, via notification service) — Slice F (final plain-text copy shipped in Slice H · S5)
- [x] Patient views: P-09 past appointments, P-13 prescription view + PDF download — Slice F
- [x] Doctor views: D-05 prescription builder — Slice F

### M4 — Launch-ready

- [x] Admin panel — doctor onboarding (A1): add doctor with initial-password set; photo upload — Slice G
- [x] Admin panel — doctor edit/deactivate (A4): edit fields, PMC/email immutability guard, deactivation-with-count warning, reactivate — Slice G
- [x] Admin panel — medicine catalogue (A2): searchable list, add, edit, deactivate — Slice G
- [x] Admin panel — alert feed (A3): payment-submitted (pending review), email failures, awaiting-prescription alerts, unhandled exceptions; email re-trigger — Slice G (payment-mismatch / refund-failure sources removed — ADR-43)
- [x] Admin panel — records & audit log (A5): unified search, detail view, set/clear `disputed`, email re-trigger — Slice G
- [x] Admin panel — platform settings (A6): min lead time, bank-transfer payment details (ADR-43), audit-logged changes — Slice G
- [x] Settings service: read/write single-row settings; audit log on every change — Slice G
- [x] Settings routes: `GET /api/admin/settings`, `PUT /api/admin/settings` — Slice G (build uses PUT full-replace)
- [ ] Error-tracking DSN wired (replace `errorTracking.js` no-op with concrete SDK)
- [x] Email automation: transport mechanism built in Slice E (outbox + dispatch worker: payment-submitted, booking confirmation, payment-not-received, 24h/1h reminders, cancellation; retry/backoff; reminder invalidation; refund-confirmation/apology removed — ADR-43); `prescription_ready` trigger built in Slice F (per-prescription via outbox `dedupe_key`); final plain-text copy for the 7 templates shipped in Slice H · S5 (`email/templates.js` shared `render()` + `formatPKR`). (HTML email + Urdu deferred per design.)
- [x] Landing page (public-facing patient acquisition surface) — Slice H · S4 (P-01 at `/`; listing relocated to `/browse`; logged-in-patient `/`→`/browse` redirect; KPI #1 `landing_view`/`booking_started` emits; ADR-35)
- [x] Legal content: `/legal/terms` and `/legal/privacy` pages — Slice H · S4 (reusable `LegalPage` banner-marked structured DRAFT; final lawyer copy is a pre-launch gate)
- [x] Admin views (client): A-01 Doctors (list / add / edit / deactivate — add-doctor and doctor-detail/edit are sub-views of A-01), A-02 Medicines, A-03 System health, A-04 Records & audit, A-05 Settings — Slice G (no A-06/A-07)
- [x] Doctor views (client): D-02 today's-appointments base view (incl. History — Today/History are in-page tabs, route-driven, mirroring the patient page; History retained as the prescription entry point; ADR-42 — 10-min Join gate, write-Rx action, awaiting badge), D-04 video consultation, D-06 cancel appointment modal — all built (D-02/D-06 prior; D-04 + D-02 Join→`/video/:id` direct route (the earlier `/ready` redirect was a bug fixed 2026-06-21) & `video_join_attempt` emit in Slice H · S3)
- [~] Patient views (client): P-11 pre-call get-ready room ✓, P-12 video consultation ✓ (Slice H · S3); P-01 landing ✓ (Slice H · S4); patient Profile tab (doc 06 §2 nav destination; no dedicated screen-inventory ID) still pending. Refund status shows within the P-08/P-09 dashboards (doc 02 §F04), not a standalone screen
- [x] Full E2E QA pass — Slice H · S7 launch gate: Playwright `e2e/` harness, 6 Critical journeys J1–J6 (11/11) vs mock adapters (ADR-38) + 3 new money-path integration tests; FK-`RESTRICT` crash class fixed (ADR-39); 320 server / 123 client / 11 e2e; verdict **Conditional-Go** (report under `docs/superpowers/reports/`). Remaining: human UAT sign-off + the vendor live-delivery gates (doc 07 §3/§10)

---

## Revision footer

| Date       | Change           | Why                                                                     |
| ---------- | ---------------- | ----------------------------------------------------------------------- |
| 2026-06-01 | Initial creation | Snapshot of build state vs. ARCH §5b module inventory + doc 02 features |
| 2026-06-03 | Corrected M1 patient/doctor view screen IDs to doc 06 canon | Slice A; M2 video screen IDs still to be aligned in Slices C/D |
| 2026-06-03 | Status sweep after Slice A merge (M1, module 1, F01, F15, frontend rows, Zod seam) | Reflect built auth backend + P-04/P-05/D-01 + client foundation |
| 2026-06-03 | Status sweep after Slice B merge (M1 ~55%, modules 3+4 → Built, F02+F09 → Built, frontend views/layouts/components, Zod seam, M1 checklist) | Reflect built discovery + availability backend + P-02/P-03/D-03 + 3 nav layouts (v1.3) |
| 2026-06-04 | Status sweep after Slice C (M1 ~85%, M2 ~40%; modules 5/6/7/8/12 → Built; F03/F04/F06 → Built; adapter/email/Zod/route rows; views→10/24, components→8/16; M1/M2 checklists) | Reflect built booking + payment (mock gateway) + refund/cancellation + P-06/P-07/P-08/P-10 (v1.4) |
| 2026-06-05 | Status sweep after Slice D (M2 ~75%; module 9 → Built; F05 → Built; appointment-evaluation worker → Built; Video adapter interface row updated; M2 checklist) | Reflect built video + appointment-evaluation worker (ADR-24/25) (v1.5) |
| 2026-06-11 | Re-pointed all file-path evidence cells to the feature-first module layout (domain merges, infra grouping, webhook split, schema domain folders, client modules); updated frontend prose (AppProviders, `buildRoutes`, session state/action split) | Folder-structure restructure (ADR-26); pure relocation, no status change (v1.6) |
| 2026-06-11 | Status sweep after Slice E (M1 ~90%, M2 ~95%; modules 8/13 updated; reconciliation + notification-dispatch + refund-retry workers → Built; payment/email adapter rows; F04/F06/F07 → Built-for-v1; M2/M4 checklists) | Reflect built F07 outbox + F04.03/F06.03 workers + real Resend adapter + G1–G4 fixes (ADR-27) (v1.7) |
| 2026-06-12 | Status sweep after Slice F (M3 → In progress ~85–90%; modules 10/11 → Built; F08 → Built, F11 → Built backend; frontend views 13/24 + P-09/P-13/D-05/D-02; client PDF renderer → Built; Zod seam; M3 checklist ticked with stale screen IDs P-10/P-11→P-09/P-13 and D-04→D-05 corrected; M4 email line `prescription_ready` built). Also corrected the stale "Video chrome (Daily SDK wrapper): Not started" row → Built in Slice D | Reflect built F08 prescriptions + F11 backend + client PDF (Slice F); video-chrome correction (v1.8) |
| 2026-06-13 | Status sweep after Slice G (M3 → Done 100%, M4 → In progress ~60%; modules 3/11/14/15/16/17 → Built; F10/F11/F12/F14/F15 → Built, F13 → Built with intentional UI gaps; cross-cutting error-tracking/Zod/route-config rows; views 18/24 + A-01..A-05; §5 M4 → In progress; M4 admin checklist ticked + settings route PATCH→PUT + admin screen-ID list corrected to A-01..A-05; M3 A-02 note) | Reflect built F10–F14 admin panel + A-01..A-05 views + DA5 reset (Slice G as-built sweep) (v1.9) |
| 2026-06-13 | Post-sweep status sync: §5 roadmap M1/M2 → In progress (match §2); §6 backlog reconciled to as-built — M1 Auth items + typed API client + session context + route config ticked (Slice A–C, missed in prior sweeps), M2 video service [~]/video-token route + audit-query API ticked, P-07/P-08/P-09 + D-02/D-03 view rows corrected to §3 canonical screen IDs; §6 intro reframed; F01 Resend note (Slice E live) | Keep the tracker synced to reality after Slice G (v1.10) |
| 2026-06-13 | Screen-ID reconciliation vs doc 06 canon: §6 M4 doctor-views row (D-05 was the built builder; history is part of D-02) and patient-views row (P-12=video, P-13=prescription per doc 06; refund status is a P-08/P-09 dashboard element, Profile tab has no inventory ID) corrected to the doc-06 registry | Align doc 13 screen IDs to the doc-06 canonical inventory (v1.11) |
| 2026-06-13 | Added a §5 "Technical follow-ups (pre/at-launch — post-Slice G)" tracker: AuditLog/Appointment index migration, Settings(id=1) prod bootstrap, Zod single-version alignment — each cross-linked to its detail doc (04/07/10/15) | Single actionable home for the deferred technical items (v1.12) |
| 2026-06-13 | Payment adapter interface → Built (PayFast PK): concrete PayFast Pakistan adapter (ADR-32, dual-channel `verifyReturn` confirm, manual refund/status degradation, two new routes, F12 manual-intervention alerts); cleared the now-stale "PayFast adapter pending (Slice H)" clauses in module 6, F04, and the M2/M4 milestone snapshots (Daily.co adapter is the remaining Slice H item) | Slice H · S1 (PayFast Pakistan adapter) (v1.13) |
| 2026-06-14 | Video adapter interface → Built (Daily.co): concrete `daily.js` adapter (ADR-33; HMAC-verified raw-body webhook → 401 + `video.webhook_rejected`, role via meeting-token `user_id` with the ADR-24 `user_name` hack removed, slot-bounded rooms); cleared the now-stale "Daily.co adapter pending" clauses in module 9 + the M2/M4 milestone snapshots (video UI P-12/D-04 is the remaining S3 item) | Slice H · S2 (Daily.co video adapter; ADR-33) (v1.14) |
| 2026-06-14 | Video UI built (ADR-34): P-11/P-12/D-04 → Built (real lazy-loaded brand-themed Daily Prebuilt iframe in one shared role-aware `VideoRoom` + get-ready `WaitingRoom`); corrected the overstated "Video chrome (Daily SDK wrapper): Built (Slice D)" row — Slice D was placeholder chrome with NO SDK, the real SDK landed in S3; reconciled D-02 (already substantially built — S3 added only the `/ready` Join redirect + `video_join_attempt` emit) and confirmed D-06 cancel modal built; module 18 Analytics/telemetry → In progress (new client `lib/analytics/track.js` KPI #3 emit seam); §2 M2 ~100% / M4 ~65% + checklists; "Last verified" → post-Slice H · S3 | Slice H · S3 (video consultation UI; ADR-34) (v1.15) |
| 2026-06-14 | Public surface built (ADR-35): P-01 landing → Built (`/`; doctor listing P-02 relocated to `/browse`; logged-in-patient `/`→`/browse` redirect; static featured grid); F16 legal pages + reusable `LegalPage` template → Built (DRAFT) (`/legal/terms`,`/legal/privacy`; final lawyer copy = pre-launch gate); module 18 Analytics → KPI #1 `landing_view`/`booking_started` client emits (`booking_confirmed` remains S6/server); module 19 Legal content + Legal-page-templates + views row (24/24) updated; route-config seam re-pointed (`/` landing + `/browse` listing + legal); §2 M4 ~75% + §6 M4 backlog landing/legal ticked; "Last verified" → post-Slice H · S4 | Slice H · S4 (public surface — landing + legal; ADR-35) (v1.16) |
| 2026-06-14 | Email automation → fully built: flipped the four "final copy is M4 / marketing copy remains" notes (M3 snapshot, module 13, §6 prescription-ready + email-automation lines) to reflect the Slice H · S5 shipped plain-text copy for all 8 templates (`email/templates.js` shared `render()` + `formatPKR`); HTML/Urdu remain deferred | Slice H · S5 (email template copy) (v1.17) |
| 2026-06-14 | Launch foundation built: module 18 Analytics/telemetry → Built (`POST /api/analytics/events` route + `analytics.record` writer + server `booking_confirmed` emit); Error-tracking seam → Built (Sentry, DSN-gated + PII scrub, ADR-36); the three §5 technical follow-ups → **Resolved** (index migration `20260613213051_slice_h_s6_indexes`, `ensureSettings()` boot bootstrap, single zod@3 via `shared` workspace + root override / duck-typing removed — ADR-37); migrations + M2 analytics checklist rows updated | Slice H · S6 (launch foundation + hardening; ADR-36/37) (v1.18) |
| 2026-06-14 | E2E QA launch gate executed: §2 M4 → ~95% (E2E executed, Conditional-Go, counts 320/123/11 + report ref); added an "E2E test harness" cross-cutting/infra row (`e2e/`, `test:e2e`, `@playwright/test`; ADR-38); §5 roadmap M4 + §6 backlog "Full E2E QA pass" ticked; added a §5 follow-up row marking the money-path FK-delete crash class **Resolved** (ADR-39, 3 new integration tests) | Slice H · S7 (E2E QA + launch gate; ADR-38/39) (v1.19) |
| 2026-06-15 | Three-role flow-audit fix cycle recorded in the §2 M4 snapshot: 13 navigable-flow issues fixed test-first; suite raised to **322 server / 135 client / 17 e2e** (+j7/j8/j9 E2E specs) | Three-role flow-audit fix session (v1.20) |
| 2026-06-15 | Re-pointed two test-file inventory paths to the centralized tree: `audit.service.test.js` → `server/test/unit/services/audit/`; the 3 money-path integration tests → `server/test/integration/` (ADR-40) | Test centralization (ADR-40); paths only, no count change |
| 2026-06-16 | F03/Slot & booking note: pending-hold recovery (Payment-pending card + Complete payment + booking-page recovery link) | Pending-hold recovery feature (34f978d) |
| 2026-06-21 | D-02 doctor-views line: corrected doctor Join→`/video/:id` direct route (earlier `/ready` redirect was a bug fixed 2026-06-21) | Role-aware doctor video-join bug fix |
| 2026-06-22 | §6 doctor-views: noted D-02 Today/History is sidebar-only (no in-page tabs; active view route-derived; ADR-41) | Doctor History sidebar-link desync bug fix (v1.24) |
| 2026-06-22 | §6 doctor-views: D-02 Today/History reinstated as in-page tabs (route-driven); sidebar simplified to Appointments · Availability (ADR-42, supersedes ADR-41) | Doctor appointments page redesign (in-page tabs) (v1.25) |
| 2026-06-28 | Manual-payment pivot (ADR-43): Payment module → manual offline bank transfer + admin accept/reject; Refund module + Payment adapter + Reconciliation/Refund-retry/Appointment-evaluation workers → Removed; Video adapter → room+token only (Daily webhook removed); Appointment state machine → 3-state (`pending`/`confirmed`/`cancelled`); Notification triggers re-listed (7 templates, refund/apology removed); Settings fallback-fee → bank-transfer details; F04/F05/F06/F07/F08 feature rows, §2 M1–M4 snapshots, §5 M2 roadmap, and §6 backlog synced; `notification-dispatch` noted as the sole cron | Manual-payment pivot — as-built sync (v1.26) |
| 2026-06-30 | F03 booking: added the single-active-appointment cap (at most one upcoming `pending`/`confirmed` per patient; `lockSlot` → `ACTIVE_LOCK_EXISTS`, replaces the No-Overlap check) + patient Cancel on `pending` Upcoming rows; server + client unit tests added (ADR-44) | Single-active-appointment limit (v1.27) |
| 2026-07-02 | §6 doctor-views: D-02 default view switched from calendar-day "Today/History" to the time-based Upcoming/Past split (mirrors the patient); pending rows now visible but inert; server + client unit tests updated (ADR-45) | Doctor Upcoming/Past bugfix (v1.28) |
